# Keep the token server-side. Do not prefix either variable with NEXT_PUBLIC_.
DIRECTUS_URL=http://localhost:8055
DIRECTUS_TOKEN=replace-with-a-static-server-token

# Use local editorial fixtures until the external Directus instance is ready.
USE_MOCK_DATA=true
